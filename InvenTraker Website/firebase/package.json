{
  "name": "@inventracker/firebase-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "set-admin-claim": "node ./set-platform-admin-claim.mjs",
    "dedupe-orgs": "node ./remove-duplicate-orgs.mjs",
    "test:rules": "firebase emulators:exec --only firestore \"vitest run\""
  },
  "dependencies": {
    "firebase-admin": "^12.7.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.3",
    "firebase": "^11.4.0",
    "vitest": "^3.0.8"
  }
}
