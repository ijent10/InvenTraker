import { DemoWorkspace } from "@/components/demo-workspace"

export const metadata = {
  title: "Demo | InvenTracker",
  description: "Try InvenTracker with fake inventory, employees, orders, policies, insights, and assistant data."
}

export default function DemoPage() {
  return <DemoWorkspace />
}
